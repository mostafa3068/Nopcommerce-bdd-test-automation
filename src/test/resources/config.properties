# Browser Configuration
browser=chrome
headless=false

# Application URLs
base.url=http://localhost:5000
# Change above to your Docker nopCommerce URL

# Timeouts (seconds)
implicit.wait=10
explicit.wait=15
page.load.timeout=30

# Test Data
default.username=standard_user@test.com
default.password=Test@123